/**
 * Process page — Processing status + Results screen (Phase 0 scaffold)
 * Full implementation: Phase 4 (ProcessingStatus) + Phase 5 (VideoPreview / BeforeAfterPlayer)
 */
export default function ProcessPage() {
    return (
        <main className="min-h-screen flex flex-col items-center justify-center p-8">
            <div className="text-center">
                <h1 className="text-3xl font-bold mb-4">Processing your video...</h1>
                <p className="text-gray-400">(Phase 4 — Processing pipeline coming soon)</p>
            </div>
        </main>
    );
}
